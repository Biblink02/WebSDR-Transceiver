# ========================================
# ===     Backend Controller Config    ===
# ========================================
# Config for the main Python/FastAPI/Socket.IO application

# 1. Web Server
# The internal port the FastAPI server will run on.
# The Kubernetes Service will target this port.
PORT=8080

# 2. Hot Pool Management
# This MUST match the 'replicas' count in your audio-worker-statefulset.yaml
POOL_SIZE=3

# The fully qualified domain name (FQDN) of the headless service
# that gives each Audio Worker its stable DNS name.
WORKER_HEADLESS_SERVICE="audio-workers-headless.default.svc.cluster.local"

# 3. Network Ports
# The ZMQ port the backend USES to send "tune" commands to the workers.
WORKER_CONTROL_PORT=5001

# The UDP port the backend LISTENS on for graphics/waterfall data.
GRAPHICS_PORT=9001

# The UDP port the backend LISTENS on for demodulated audio streams.
AUDIO_PORT=9002


# ========================================
# ===       SDR Server Config          ===
# ========================================
# Config for the I/Q stream source.
# These variables are used by the Audio and Graphics workers.

# The Kubernetes service name for the SDR server.
SDR_HOST="sdr-server"

# The TCP port the SDR server streams I/Q data on.
SDR_PORT=5000


# ========================================
# ===       Worker Config              ===
# ========================================
# Config for where the workers should send their data.
# These variables are used by the Audio and Graphics workers.

# The Kubernetes service name for the backend controller.
BACKEND_HOST="backend-controller"

# The destination port for graphics data (must match GRAPHICS_PORT).
BACKEND_GRAPHICS_PORT=9001

# The destination port for audio data (must match AUDIO_PORT).
BACKEND_AUDIO_PORT=9002